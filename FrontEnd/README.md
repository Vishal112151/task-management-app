# Task Management System

Angular 20 frontend scaffold for a Task Management System.

## Features

- Standalone Angular components
- Angular Material UI
- Feature folders for `auth`, `tasks`, `shared`, and `core`
- JWT-ready auth service and HTTP interceptor
- Enterprise-style folder layout

## Commands

- `npm install`
- `npm start`
- `npm run build`
- `npm test`
